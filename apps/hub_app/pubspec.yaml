name: hub_app
description: "HubSync Hub App (Specialists + Courier roles)."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

# Phase 0 scaffold: bare minimum for CI to run.
# Riverpod 2.x and Drift are added in Phase 1 (ROADMAP 0.1) once CI is green.
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

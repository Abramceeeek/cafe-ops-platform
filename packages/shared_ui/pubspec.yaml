name: shared_ui
description: Shared HubSync UI — adaptive scaffold, responsive helpers, status badge.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  shared_constants:
    path: ../shared_constants

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
